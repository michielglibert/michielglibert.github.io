import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fonts: {
    heading: "Manrope",
    body: "Manrope",
  },
  colors: {
    blue: "#007bff",
  },
});

export default theme;
