import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fonts: {
    heading: "Manrope",
    body: "Manrope",
  },
  colors: {
    blue: {
      500: "#007bff",
    },
  },
});

export default theme;
